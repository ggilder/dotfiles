#!/bin/sh
set -e

usage() {
	echo "Usage:"
	echo "\tsave shortcut_name"
}

if [ $# -lt 1 ]; then
	usage
	exit
fi

if [ $# -gt 1 ]; then
	echo "Too many arguments!\n"
	usage
	exit
fi


echo $@ | grep "^[a-zA-Z]\w*$"
